import {SegmentProgressionColorPipe} from './segment-progression-color.pipe';

describe('SegmentProgressionColorPipe', () => {
    it('create an instance', () => {
        const pipe = new SegmentProgressionColorPipe();
        expect(pipe).toBeTruthy();
    });
});
